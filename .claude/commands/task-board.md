# /task:board

## MANDATORY: Use the task-query Skill

**You MUST use the `task-query` skill located at `.claude/skills/task-query/SKILL.md`**

## Before Starting

1. **Announce**: "I'm using task-query to render a kanban-style task board"
2. **Read the skill**: Load `.claude/skills/task-query/SKILL.md`
3. **Follow exactly**: Execute the skill as written

## Purpose

Kanban-style board view: columns organized by status (backlog, todo, in-progress, done), rows grouped by queue. Provides a visual overview of all tasks across the system.

## Arguments

None. Renders the full board automatically.

## No Rationalization

You MUST follow the referenced skill exactly. No shortcuts.
