# PM-OS Document Sync Configuration
# Generated by setup_doc_sync.sh — edit as needed.
#
# onedrive_root: Path to your OneDrive sync folder (auto-detected by setup)
# sharepoint_site: Subfolder name inside OneDrive for PM-OS docs
# sync_paths: Glob patterns for files to sync (relative to pm-os/)
# sync_exclude: Glob patterns to exclude from sync

onedrive_root: "~/Library/CloudStorage/OneDrive-Vantaca,LLC/"
sharepoint_site: "PM-OS"
sync_enabled: true

sync_paths:
  - datasets/product/**/*.md
  - datasets/research/**/*.md
  - datasets/strategy/**/*.md

sharepoint_tenant_url: "https://vantaca-my.sharepoint.com"
sharepoint_doc_root: "/personal/jay_jenkins_vantaca_com/Documents"

sync_exclude:
  - datasets/meetings/**
  - datasets/tasks/**
  - datasets/learning/**
